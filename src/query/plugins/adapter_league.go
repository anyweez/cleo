import "gamelog"

func Produce(filter_name string, params ...string) {
	switch {
		case filter_name == "outcome":
			// return a function that accepts a single string param and returns a bool
		case filter_name == "champion":
			// return a function that accepts a single string param and returns a bool		
	}
}
